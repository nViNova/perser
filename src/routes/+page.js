// export const load = async ({ url, fetch }) => {
// 	const postRes = await fetch(`${url.origin}/api/posts.json`);
// 	const posts = await postRes.json();

// 	const featureRes = await fetch(`${url.origin}/api/features.json`);
// 	const features = await featureRes.json();

// 	const newsRes = await fetch(`${url.origin}/api/news.json`);
// 	const news = await newsRes.json();

// 	const totalRes = await fetch(`${url.origin}/api/posts/count`);
// 	const total = await totalRes.json();

// 	return { posts, total, features, news };
// };
